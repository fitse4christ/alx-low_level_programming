#include <stdio.h>
/**
 * main - entry point
 * Description print all single digit number of base 10
 * Return: 0 
 */
int main(void)
{
	int c = 0;

	while (c < 0)
	{
		putchar(c + '0');
		c++;
	}

	putchar('\n');
	return (0);
}
